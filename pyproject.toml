[build-system]
requires = [
    "setuptools>=38.6.0,!=50.0.0",
    "wheel",
    "Cython>=0.29,!=0.29.18,!=0.29.31",
    "numpy>=1.21.2"
]
build-backend = "setuptools.build_meta"
backend-path = ["."] # もしsetuptoolsがプロジェクトルートのものを参照する場合